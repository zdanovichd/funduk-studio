/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: 'swiperjs.com',
        },
      ],
    },
  };

  export default nextConfig;

//   https://placehold.co/600x400/